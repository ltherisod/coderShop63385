const ItemListContainer = ({greeting, texto}) => {
    // const {greeting, texto} = props
    return(
        <div>
            <h1>{greeting}</h1>
            <p>{texto}</p>
        </div>
    )
}
export default ItemListContainer